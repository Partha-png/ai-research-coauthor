# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# LangChain utilities (used for RAG chain helpers)
langchain>=0.2.0
langchain-aws>=0.1.0
langchain-core>=0.2.0

# Vector store + embeddings (FAISS for local RAG)
faiss-cpu>=1.7.4
numpy>=1.26.0

# Streamlit UI
streamlit>=1.35.0

# HTTP + Atom/RSS feeds (arXiv API)
requests>=2.31.0
feedparser>=6.0.11

# PDF parsing
PyPDF2>=3.0.1

# Environment variable management
python-dotenv>=1.0.0

# Standard library (no install needed): uuid, json, decimal, pathlib, logging
